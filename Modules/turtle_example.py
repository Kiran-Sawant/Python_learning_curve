from turtle import * #imports everything without an underscore
                     #with this import you do not need to type the name of module before its methods
                     #however, the IDE does not recognize its methods either
# import time
 
forward(150)
right(250)
forward(142)
circle(100)
done()
# time.sleep(5) #keeps the window open for 5 seconds